I will perform a comprehensive refactoring of the Stock Management system to improve accuracy, maintainability, and user experience.

### 1. Backend Refactoring & Business Logic
- **Create `StockCalculationService`**:
  - Implement precise calculation methods that correctly handle both simple products and variant products.
  - **Fix Valuation Bug**: Currently, variant products are valued using the *parent product's price*. I will fix this to use `SUM(variant_stock * variant_price)` for accuracy.
  - Implement optimized SQL queries to calculate Total Value, Total Cost, and Potential Profit in a single database round-trip, avoiding memory-intensive PHP loops.
- **Optimize `StockManager` Component**:
  - Inject the new service.
  - Implement **Lazy Loading** for the statistics dashboard to ensure the page loads instantly, with stats fetching in the background.
  - Consolidate the duplicate logic found between `Index.php` and `StockManager.php`.

### 2. UI/UX Redesign
- **Standardize Interface**:
  - Rewrite `stock-manager.blade.php` to use the project's standard `x-ui` components (Cards, Badges, Buttons) for consistency with the rest of the Admin panel.
  - Implement a clean **Data Table** with sortable columns and filters.
- **Interaction Improvements**:
  - **Expandable Rows**: Keep and improve the variant expansion interaction.
  - **Unified Modals**: Standardize "Quick Adjust" and "Bulk Adjust" modals with better validation and feedback.
  - **Audit Trail**: Integrate the history view directly into the main interface (as a tab) with improved readability (user, reason, timestamp).

### 3. Testing & Verification
- **Unit Tests**: Create `tests/Unit/Services/StockCalculationServiceTest.php` to verify:
  - Valuation logic for products with and without variants.
  - Handling of edge cases (zero stock, negative stock adjustments).
- **Feature Tests**: Create `tests/Feature/Livewire/Stock/StockManagerTest.php` to ensure:
  - The page renders correctly.
  - Adjustments (In/Out) update the database correctly.
  - Search and Filters work as expected.

### 4. Implementation Steps
1.  **Service Layer**: Create `StockCalculationService` and write Unit Tests first (TDD).
2.  **Component Refactor**: Update `StockManager.php` to use the service and add caching/lazy loading.
3.  **View Reconstruction**: Rebuild the Blade view using `x-ui` components.
4.  **Final Verification**: Run full test suite and manual verification.